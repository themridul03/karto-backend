generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model User {
  id                      String                 @id @default(uuid())
  fullName                String?
  email                   String                 @unique
  phone                   String?                @unique
  password                String?
  avatarUrl               String?
  role                    Role                   @default(CUSTOMER)
  isActive                Boolean                @default(true)
  createdAt               DateTime               @default(now())
  updatedAt               DateTime               @updatedAt
  address                 String?
  cityId                  String?
  refreshToken            String?
  vehicleNo               String?
  vehicleType             String?
  referralCode            String?                @unique
  aadhaarImageUrl         String?
  aadhaarNumber           String?
  drivingLicense          String?
  isOnline                Boolean                @default(false)
  kycStatus               String                 @default("PENDING")
  lastSeen                DateTime?
  licenseImageUrl         String?
  blockedAt               DateTime?
  blockedReason           String?
  deletedAt               DateTime?
  kycReviewedAt           DateTime?
  kycRejectionReason      String?
  emailVerifiedAt         DateTime?
  phoneVerifiedAt         DateTime?
  cartItems               CartItem[]
  couponRedemptions       CouponRedemption[]
  menuItemReviews         MenuItemReview[]
  notifications           Notification[]
  riderOrders             Order[]                @relation("RiderOrders")
  customerOrders          Order[]                @relation("CustomerOrders")
  vendorOrders            Order[]                @relation("VendorOrders")
  changedOrderStatuses    OrderStatusHistory[]   @relation("ChangedByUser")
  pushTokens              PushToken[]
  recentViews             RecentlyViewedItem[]
  referralsReceived       Referral[]             @relation("ReferralsReceived")
  referralsMade           Referral[]             @relation("ReferralsMade")
  restaurants             Restaurant[]
  ratings                 RestaurantRating[]
  riderCoupons            RiderCoupon[]
  riderEarnings           RiderEarning[]
  riderIncentives         RiderIncentive[]
  riderLocations          RiderLocation[]
  riderSettlements        RiderSettlement[]      @relation("RiderSettlementUser")
  riderWallet             RiderWallet?
  supportMessages         SupportMessage[]
  assignedSupportTickets  SupportTicket[]        @relation("AssignedSupportTickets")
  supportTickets          SupportTicket[]        @relation("CustomerSupportTickets")
  city                    City?                  @relation(fields: [cityId], references: [id])
  addresses               UserAddress[]
  favorites               UserFavorite[]
  favoriteItems           UserFavoriteItem[]
  wallet                  UserWallet?
  vendorSettlements       VendorSettlement[]     @relation("VendorSettlementUser")
  walletTransactions      WalletTransaction[]
  adminProfile            AdminProfile?          @relation("AdminProfileUser")
  adminAuditLogs          AdminAuditLog[]        @relation("AdminAuditActor")
  processedRefunds        Refund[]               @relation("RefundProcessedByAdmin")
  vendorInvoices          VendorInvoice[]        @relation("VendorInvoiceUser")
  preferences             UserPreference?
  authSessions            AuthSession[]
  savedPaymentMethods     SavedPaymentMethod[]
  searchHistory           SearchHistory[]
  riderProfile            RiderProfile?          @relation("RiderProfileUser")
  riderDocuments          RiderDocument[]        @relation("RiderDocumentRider")
  reviewedRiderDocuments  RiderDocument[]        @relation("RiderDocumentReviewer")
  riderRatingsReceived    RiderRating[]          @relation("RatedRider")
  riderRatingsGiven       RiderRating[]          @relation("RiderRatingCustomer")
  riderAvailabilityLogs   RiderAvailabilityLog[] @relation("RiderAvailabilityUser")
  restaurantMemberships   RestaurantMember[]
  reviewedVendorDocuments VendorDocument[]       @relation("VendorDocumentReviewer")

  @@index([role])
  @@index([email])
  @@index([phone])
  @@index([cityId])
  @@index([referralCode])
  @@index([isActive])
  @@index([createdAt])
  @@index([deletedAt])
}

model Otp {
  id        String   @id @default(uuid())
  email     String?
  phone     String?
  code      String
  expiresAt DateTime
  verified  Boolean  @default(false)
  createdAt DateTime @default(now())

  @@index([email])
  @@index([phone])
  @@index([code])
}

model City {
  id          String       @id @default(uuid())
  name        String       @db.VarChar(100)
  code        String       @unique @db.VarChar(50)
  isActive    Boolean      @default(true)
  createdAt   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt
  coupons     Coupon[]
  restaurants Restaurant[]
  users       User[]

  @@index([code])
  @@index([name])
  @@index([isActive])
}

model Category {
  id            String               @id @default(uuid())
  name          String               @db.VarChar(60)
  description   String?              @db.VarChar(200)
  imageUrl      String?              @db.VarChar(550)
  createdAt     DateTime             @default(now())
  updatedAt     DateTime             @updatedAt
  menuItems     MenuItem[]
  subCategories ProductSubCategory[]
  restaurants   Restaurant[]

  @@index([name])
}

model ProductSubCategory {
  id          String     @id @default(uuid())
  categoryId  String
  name        String     @db.VarChar(100)
  description String?
  imageUrl    String?
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt
  menuItems   MenuItem[]
  category    Category   @relation(fields: [categoryId], references: [id], onDelete: Cascade)

  @@index([categoryId])
  @@index([name])
}

model VendorCategory {
  id            String              @id @default(uuid())
  restaurantId  String
  name          String              @db.VarChar(100)
  description   String?
  imageUrl      String?             @db.VarChar(550)
  isActive      Boolean             @default(true)
  createdAt     DateTime            @default(now())
  updatedAt     DateTime            @updatedAt
  menuItems     MenuItem[]
  restaurant    Restaurant          @relation(fields: [restaurantId], references: [id], onDelete: Cascade)
  subCategories VendorSubCategory[]

  @@index([restaurantId])
  @@index([name])
  @@index([isActive])
}

model VendorSubCategory {
  id               String         @id @default(uuid())
  vendorCategoryId String
  name             String         @db.VarChar(100)
  description      String?
  imageUrl         String?        @db.VarChar(550)
  isActive         Boolean        @default(true)
  createdAt        DateTime       @default(now())
  updatedAt        DateTime       @updatedAt
  menuItems        MenuItem[]
  vendorCategory   VendorCategory @relation(fields: [vendorCategoryId], references: [id], onDelete: Cascade)

  @@index([vendorCategoryId])
  @@index([name])
  @@index([isActive])
}

model Restaurant {
  id                  String                     @id @default(uuid())
  vendorId            String?
  categoryId          String?
  name                String                     @db.VarChar(255)
  address             String                     @db.VarChar(255)
  phone               String                     @db.VarChar(255)
  email               String                     @unique @db.VarChar(255)
  imageUrl            String?                    @db.VarChar(550)
  rating              Decimal                    @default(0) @db.Decimal(3, 2)
  totalReviews        Int                        @default(0)
  deliveryFee         Decimal                    @default(5) @db.Decimal(10, 2)
  minimumOrder        Decimal                    @default(0) @db.Decimal(10, 2)
  deliveryTime        String                     @default("30-45 mins") @db.VarChar(50)
  isOpen              Boolean                    @default(true)
  isFeatured          Boolean                    @default(false)
  createdAt           DateTime                   @default(now())
  updatedAt           DateTime                   @updatedAt
  cityId              String?
  commission          Decimal                    @default(0) @db.Decimal(5, 2)
  ownerMobileNo       String?                    @db.VarChar(20)
  ownerName           String?                    @db.VarChar(255)
  type                String?                    @db.VarChar(80)
  autoAccept          Boolean                    @default(false)
  bankAccount         String?
  bankName            String?
  busyUntil           DateTime?
  closingTime         String?
  defaultPrepTime     Int                        @default(30)
  fssaiNumber         String?
  gstNumber           String?
  ifscCode            String?
  isAcceptingOrders   Boolean                    @default(true)
  isPureVeg           Boolean                    @default(false)
  isVerified          Boolean                    @default(false)
  openingTime         String?
  weeklyOffDay        String?
  latitude            Decimal?                   @db.Decimal(10, 7)
  longitude           Decimal?                   @db.Decimal(10, 7)
  bannerUrl           String?                    @db.VarChar(550)
  verificationStatus  VendorVerificationStatus   @default(PENDING)
  verifiedAt          DateTime?
  verificationNote    String?
  suspendedAt         DateTime?
  suspensionReason    String?
  deletedAt           DateTime?
  cartItems           CartItem[]
  coupons             Coupon[]
  menuItems           MenuItem[]
  orders              Order[]
  orderItems          OrderItem[]
  category            Category?                  @relation(fields: [categoryId], references: [id])
  city                City?                      @relation(fields: [cityId], references: [id])
  vendor              User?                      @relation(fields: [vendorId], references: [id])
  ratings             RestaurantRating[]
  timings             RestaurantTiming[]
  favorites           UserFavorite[]
  vendorCategories    VendorCategory[]
  vendorSettlements   VendorSettlement[]
  vendorInvoices      VendorInvoice[]
  members             RestaurantMember[]
  documents           VendorDocument[]
  payoutAccount       VendorPayoutAccount?
  operatingExceptions VendorOperatingException[]

  @@index([vendorId])
  @@index([cityId])
  @@index([categoryId])
  @@index([isFeatured])
  @@index([isOpen])
  @@index([rating])
  @@index([type])
  @@index([verificationStatus])
  @@index([isVerified])
  @@index([isAcceptingOrders])
  @@index([createdAt])
  @@index([deletedAt])
}

model RestaurantTiming {
  id           String     @id @default(uuid())
  restaurantId String
  dayOfWeek    Int
  openTime     String
  closeTime    String
  isClosed     Boolean    @default(false)
  restaurant   Restaurant @relation(fields: [restaurantId], references: [id], onDelete: Cascade)

  @@unique([restaurantId, dayOfWeek])
  @@index([restaurantId])
}

model MenuItem {
  id                  String                  @id @default(uuid())
  restaurantId        String
  name                String                  @db.VarChar(255)
  description         String?
  price               Decimal                 @db.Decimal(10, 2)
  imageUrl            String?                 @db.VarChar(550)
  isVegetarian        Boolean                 @default(false)
  isPopular           Boolean                 @default(false)
  isAvailable         Boolean                 @default(true)
  createdAt           DateTime                @default(now())
  updatedAt           DateTime                @updatedAt
  categoryId          String?
  subCategoryId       String?
  calories            Int?
  isBestSeller        Boolean                 @default(false)
  isVeg               Boolean                 @default(true)
  prepTimeMin         Int?                    @default(20)
  rating              Decimal                 @default(0) @db.Decimal(3, 2)
  servingInfo         String?
  spiceLevel          Int?                    @default(0)
  totalReviews        Int                     @default(0)
  vendorCategoryId    String?
  vendorSubCategoryId String?
  cartItems           CartItem[]
  category            Category?               @relation(fields: [categoryId], references: [id])
  restaurant          Restaurant              @relation(fields: [restaurantId], references: [id], onDelete: Cascade)
  subCategory         ProductSubCategory?     @relation(fields: [subCategoryId], references: [id])
  vendorCategory      VendorCategory?         @relation(fields: [vendorCategoryId], references: [id])
  vendorSubCategory   VendorSubCategory?      @relation(fields: [vendorSubCategoryId], references: [id])
  addons              MenuItemAddon[]
  customizations      MenuItemCustomization[]
  reviews             MenuItemReview[]
  orderItems          OrderItem[]
  recentViews         RecentlyViewedItem[]
  favoriteBy          UserFavoriteItem[]

  @@index([restaurantId])
  @@index([categoryId])
  @@index([subCategoryId])
  @@index([vendorCategoryId])
  @@index([vendorSubCategoryId])
  @@index([isPopular])
  @@index([isBestSeller])
  @@index([isAvailable])
  @@index([isVegetarian])
  @@index([isVeg])
}

model MenuItemCustomization {
  id         String   @id @default(uuid())
  menuItemId String
  title      String   @db.VarChar(120)
  price      Decimal  @default(0) @db.Decimal(10, 2)
  isRequired Boolean  @default(false)
  isActive   Boolean  @default(true)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  menuItem   MenuItem @relation(fields: [menuItemId], references: [id], onDelete: Cascade)

  @@index([menuItemId])
  @@index([isActive])
}

model MenuItemAddon {
  id         String   @id @default(uuid())
  menuItemId String
  title      String   @db.VarChar(120)
  price      Decimal  @db.Decimal(10, 2)
  imageUrl   String?
  isActive   Boolean  @default(true)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  menuItem   MenuItem @relation(fields: [menuItemId], references: [id], onDelete: Cascade)

  @@index([menuItemId])
  @@index([isActive])
}

model MenuItemReview {
  id         String   @id @default(uuid())
  userId     String
  menuItemId String
  orderId    String?
  rating     Int
  review     String?
  isActive   Boolean  @default(true)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  menuItem   MenuItem @relation(fields: [menuItemId], references: [id], onDelete: Cascade)
  order      Order?   @relation(fields: [orderId], references: [id])
  user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([menuItemId])
  @@index([userId])
  @@index([orderId])
  @@index([isActive])
}

model UserAddress {
  id        String   @id @default(uuid())
  userId    String
  address   String
  country   String   @default("India") @db.VarChar(100)
  city      String?  @db.VarChar(100)
  latitude  Decimal? @db.Decimal(10, 7)
  longitude Decimal? @db.Decimal(10, 7)
  isDefault Boolean  @default(false)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  label     String   @default("Home") @db.VarChar(50)
  landmark  String?  @db.VarChar(150)
  pincode   String?  @db.VarChar(20)
  state     String?  @db.VarChar(100)
  orders    Order[]
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([city])
}

model CartItem {
  id                String     @id @default(uuid())
  userId            String
  menuItemId        String
  restaurantId      String
  quantity          Int        @default(1)
  price             Decimal    @db.Decimal(10, 2)
  totalPrice        Decimal    @db.Decimal(10, 2)
  note              String?
  createdAt         DateTime   @default(now())
  addonJson         Json?
  customizationJson Json?
  variantKey        String     @default("") @db.VarChar(120)
  menuItem          MenuItem   @relation(fields: [menuItemId], references: [id], onDelete: Cascade)
  restaurant        Restaurant @relation(fields: [restaurantId], references: [id], onDelete: Cascade)
  user              User       @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, menuItemId, variantKey])
  @@index([userId])
  @@index([restaurantId])
  @@index([menuItemId])
}

model Order {
  id                          String               @id @default(uuid())
  userId                      String
  restaurantId                String
  vendorId                    String?
  riderId                     String?
  addressId                   String?
  totalAmount                 Decimal              @db.Decimal(10, 2)
  deliveryFee                 Decimal              @default(5) @db.Decimal(10, 2)
  paymentMethod               PaymentMethod        @default(COD)
  paymentStatus               PaymentStatus        @default(PENDING)
  status                      OrderStatus          @default(PLACED)
  orderNumber                 String               @unique @db.VarChar(50)
  customerNote                String?
  acceptedAt                  DateTime?
  preparingAt                 DateTime?
  readyAt                     DateTime?
  pickedAt                    DateTime?
  deliveredAt                 DateTime?
  cancelledAt                 DateTime?
  createdAt                   DateTime             @default(now())
  updatedAt                   DateTime             @updatedAt
  estimatedPreparationMinutes Int?
  cancelReason                String?
  cancelledBy                 String?
  couponId                    String?
  discount                    Decimal              @default(0) @db.Decimal(10, 2)
  itemTotal                   Decimal              @default(0) @db.Decimal(10, 2)
  refundAmount                Decimal?             @db.Decimal(10, 2)
  taxAmount                   Decimal              @default(0) @db.Decimal(10, 2)
  cgstAmount                  Decimal              @default(0) @db.Decimal(10, 2)
  cgstRate                    Decimal              @default(2.5) @db.Decimal(5, 2)
  platformFee                 Decimal              @default(0) @db.Decimal(10, 2)
  sgstAmount                  Decimal              @default(0) @db.Decimal(10, 2)
  sgstRate                    Decimal              @default(2.5) @db.Decimal(5, 2)
  distanceKm                  Decimal?             @db.Decimal(10, 2)
  commissionableAmount        Decimal?             @db.Decimal(10, 2)
  commissionRate              Decimal?             @db.Decimal(5, 2)
  platformCommissionAmount    Decimal?             @db.Decimal(10, 2)
  vendorSettlementAmount      Decimal?             @db.Decimal(10, 2)
  deliveryOtp                 String?
  deliveryOtpExpiresAt        DateTime?
  deliveryOtpVerified         Boolean              @default(false)
  couponRedemptions           CouponRedemption[]
  menuItemReviews             MenuItemReview[]
  address                     UserAddress?         @relation(fields: [addressId], references: [id])
  coupon                      Coupon?              @relation(fields: [couponId], references: [id])
  restaurant                  Restaurant           @relation(fields: [restaurantId], references: [id])
  rider                       User?                @relation("RiderOrders", fields: [riderId], references: [id])
  user                        User                 @relation("CustomerOrders", fields: [userId], references: [id])
  vendor                      User?                @relation("VendorOrders", fields: [vendorId], references: [id])
  items                       OrderItem[]
  history                     OrderStatusHistory[]
  riderCoupons                RiderCoupon[]
  riderEarnings               RiderEarning[]
  riderLocations              RiderLocation[]
  riderSettlements            RiderSettlement[]
  vendorSettlements           VendorSettlement[]
  paymentTransactions         PaymentTransaction[]
  refunds                     Refund[]
  supportTickets              SupportTicket[]
  vendorInvoiceItems          VendorInvoiceItem[]
  riderRating                 RiderRating?

  @@index([userId])
  @@index([restaurantId])
  @@index([vendorId])
  @@index([riderId])
  @@index([status])
  @@index([paymentStatus])
  @@index([createdAt])
  @@index([couponId])
  @@index([paymentMethod])
  @@index([deliveredAt])
  @@index([cancelledAt])
  @@index([restaurantId, status, createdAt])
  @@index([restaurantId, paymentStatus, createdAt])
  @@index([riderId, status, deliveredAt])
}

model OrderItem {
  id                String      @id @default(uuid())
  orderId           String
  menuItemId        String?
  restaurantId      String?
  quantity          Int
  price             Decimal     @db.Decimal(10, 2)
  totalPrice        Decimal     @db.Decimal(10, 2)
  itemName          String?
  createdAt         DateTime    @default(now())
  addonJson         Json?
  customizationJson Json?
  menuItem          MenuItem?   @relation(fields: [menuItemId], references: [id])
  order             Order       @relation(fields: [orderId], references: [id], onDelete: Cascade)
  restaurant        Restaurant? @relation(fields: [restaurantId], references: [id])

  @@index([orderId])
  @@index([menuItemId])
  @@index([restaurantId])
}

model OrderStatusHistory {
  id            String      @id @default(uuid())
  orderId       String
  status        OrderStatus
  changedBy     String?
  note          String?
  createdAt     DateTime    @default(now())
  changedByUser User?       @relation("ChangedByUser", fields: [changedBy], references: [id])
  order         Order       @relation(fields: [orderId], references: [id], onDelete: Cascade)

  @@index([orderId])
  @@index([changedBy])
  @@index([status])
}

model RiderLocation {
  id        String   @id @default(uuid())
  riderId   String
  orderId   String
  latitude  Decimal  @db.Decimal(10, 7)
  longitude Decimal  @db.Decimal(10, 7)
  updatedAt DateTime @updatedAt
  order     Order    @relation(fields: [orderId], references: [id], onDelete: Cascade)
  rider     User     @relation(fields: [riderId], references: [id], onDelete: Cascade)

  @@unique([riderId, orderId])
  @@index([orderId])
  @@index([riderId])
}

model RiderWallet {
  id        String   @id @default(uuid())
  riderId   String   @unique
  balance   Decimal  @default(0) @db.Decimal(10, 2)
  todayEarn Decimal  @default(0) @db.Decimal(10, 2)
  totalEarn Decimal  @default(0) @db.Decimal(10, 2)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  rider     User     @relation(fields: [riderId], references: [id], onDelete: Cascade)
}

model RiderEarning {
  id        String   @id @default(uuid())
  riderId   String
  orderId   String
  amount    Decimal  @db.Decimal(10, 2)
  note      String?
  createdAt DateTime @default(now())
  order     Order    @relation(fields: [orderId], references: [id], onDelete: Cascade)
  rider     User     @relation(fields: [riderId], references: [id], onDelete: Cascade)

  @@unique([riderId, orderId])
  @@index([orderId])
  @@index([createdAt])
}

model RiderCoupon {
  id        String   @id @default(uuid())
  riderId   String
  orderId   String
  code      String   @unique
  title     String
  amount    Decimal  @db.Decimal(10, 2)
  message   String
  createdAt DateTime @default(now())
  order     Order    @relation(fields: [orderId], references: [id], onDelete: Cascade)
  rider     User     @relation(fields: [riderId], references: [id], onDelete: Cascade)

  @@index([riderId])
  @@index([orderId])
}

model RestaurantRating {
  id           String     @id @default(uuid())
  restaurantId String
  userId       String
  rating       Int
  review       String?
  createdAt    DateTime   @default(now())
  updatedAt    DateTime   @updatedAt
  isActive     Boolean    @default(true)
  restaurant   Restaurant @relation(fields: [restaurantId], references: [id], onDelete: Cascade)
  user         User       @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([restaurantId, userId])
  @@index([isActive])
  @@index([restaurantId])
  @@index([userId])
}

model UserFavorite {
  id           String     @id @default(uuid())
  userId       String
  restaurantId String
  createdAt    DateTime   @default(now())
  restaurant   Restaurant @relation(fields: [restaurantId], references: [id], onDelete: Cascade)
  user         User       @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, restaurantId])
  @@index([userId])
  @@index([restaurantId])
}

model UserFavoriteItem {
  id         String   @id @default(uuid())
  userId     String
  menuItemId String
  createdAt  DateTime @default(now())
  menuItem   MenuItem @relation(fields: [menuItemId], references: [id], onDelete: Cascade)
  user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, menuItemId])
  @@index([userId])
  @@index([menuItemId])
}

model RecentlyViewedItem {
  id         String   @id @default(uuid())
  userId     String
  menuItemId String
  viewedAt   DateTime @default(now())
  menuItem   MenuItem @relation(fields: [menuItemId], references: [id], onDelete: Cascade)
  user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, menuItemId])
  @@index([userId])
  @@index([menuItemId])
  @@index([viewedAt])
}

model Discount {
  id              String    @id @default(uuid())
  title           String    @db.VarChar(100)
  description     String?
  imageUrl        String?
  discountPercent Int
  validUntil      DateTime?
  isActive        Boolean   @default(true)
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt

  @@index([isActive])
  @@index([validUntil])
}

model Coupon {
  id                String             @id @default(uuid())
  code              String             @unique @db.VarChar(50)
  title             String             @db.VarChar(120)
  description       String?
  type              CouponType
  scope             CouponScope        @default(GLOBAL)
  value             Decimal            @db.Decimal(10, 2)
  maxDiscount       Decimal?           @db.Decimal(10, 2)
  minOrder          Decimal?           @db.Decimal(10, 2)
  restaurantId      String?
  cityId            String?
  usageLimit        Int?
  perUserUsageLimit Int?               @default(1)
  usedCount         Int                @default(0)
  validFrom         DateTime?
  validUntil        DateTime?
  isActive          Boolean            @default(true)
  createdAt         DateTime           @default(now())
  updatedAt         DateTime           @updatedAt
  city              City?              @relation(fields: [cityId], references: [id])
  restaurant        Restaurant?        @relation(fields: [restaurantId], references: [id], onDelete: Cascade)
  redemptions       CouponRedemption[]
  orders            Order[]

  @@index([code])
  @@index([isActive])
  @@index([restaurantId])
  @@index([cityId])
  @@index([validUntil])
}

model CouponRedemption {
  id             String   @id @default(uuid())
  couponId       String
  userId         String
  orderId        String
  discountAmount Decimal  @db.Decimal(10, 2)
  createdAt      DateTime @default(now())
  coupon         Coupon   @relation(fields: [couponId], references: [id], onDelete: Cascade)
  order          Order    @relation(fields: [orderId], references: [id], onDelete: Cascade)
  user           User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([couponId, orderId])
  @@index([couponId])
  @@index([userId])
  @@index([orderId])
}

model Referral {
  id             String         @id @default(uuid())
  referrerId     String
  referredUserId String?
  referralCode   String
  status         ReferralStatus @default(PENDING)
  rewardAmount   Decimal?       @db.Decimal(10, 2)
  rewardedAt     DateTime?
  createdAt      DateTime       @default(now())
  updatedAt      DateTime       @updatedAt
  referredUser   User?          @relation("ReferralsReceived", fields: [referredUserId], references: [id])
  referrer       User           @relation("ReferralsMade", fields: [referrerId], references: [id], onDelete: Cascade)

  @@index([referrerId])
  @@index([referredUserId])
  @@index([referralCode])
  @@index([status])
}

model UserWallet {
  id           String              @id @default(uuid())
  userId       String              @unique
  balance      Decimal             @default(0) @db.Decimal(10, 2)
  totalCredit  Decimal             @default(0) @db.Decimal(10, 2)
  totalDebit   Decimal             @default(0) @db.Decimal(10, 2)
  createdAt    DateTime            @default(now())
  updatedAt    DateTime            @updatedAt
  user         User                @relation(fields: [userId], references: [id], onDelete: Cascade)
  transactions WalletTransaction[]
}

model WalletTransaction {
  id          String                @id @default(uuid())
  userId      String
  walletId    String?
  type        WalletTransactionType
  amount      Decimal               @db.Decimal(10, 2)
  title       String?
  description String?
  referenceId String?
  createdAt   DateTime              @default(now())
  user        User                  @relation(fields: [userId], references: [id], onDelete: Cascade)
  wallet      UserWallet?           @relation(fields: [walletId], references: [id])

  @@index([userId])
  @@index([walletId])
  @@index([type])
  @@index([createdAt])
}

model PushToken {
  id        String   @id @default(uuid())
  userId    String
  token     String   @unique
  platform  String?
  deviceId  String?
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([token])
  @@index([isActive])
}

model Notification {
  id        String           @id @default(uuid())
  userId    String?
  type      NotificationType @default(SYSTEM)
  title     String
  body      String
  data      Json?
  imageUrl  String?
  deepLink  String?
  isRead    Boolean          @default(false)
  readAt    DateTime?
  sentAt    DateTime?
  createdAt DateTime         @default(now())
  user      User?            @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([type])
  @@index([isRead])
  @@index([createdAt])
  @@index([sentAt])
}

model SupportTicket {
  id              String                @id @default(uuid())
  userId          String
  assignedAdminId String?
  subject         String
  message         String?
  status          SupportTicketStatus   @default(OPEN)
  priority        SupportTicketPriority @default(MEDIUM)
  orderId         String?
  tags            Json?
  lastReplyAt     DateTime?
  resolvedAt      DateTime?
  closedAt        DateTime?
  createdAt       DateTime              @default(now())
  updatedAt       DateTime              @updatedAt
  messages        SupportMessage[]
  order           Order?                @relation(fields: [orderId], references: [id], onDelete: SetNull)
  assignedAdmin   User?                 @relation("AssignedSupportTickets", fields: [assignedAdminId], references: [id])
  user            User                  @relation("CustomerSupportTickets", fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([assignedAdminId])
  @@index([status])
  @@index([priority])
  @@index([orderId])
  @@index([createdAt])
  @@index([updatedAt])
}

model SupportMessage {
  id        String        @id @default(uuid())
  ticketId  String
  senderId  String
  message   String
  imageUrl  String?
  createdAt DateTime      @default(now())
  sender    User          @relation(fields: [senderId], references: [id], onDelete: Cascade)
  ticket    SupportTicket @relation(fields: [ticketId], references: [id], onDelete: Cascade)

  @@index([ticketId])
  @@index([senderId])
}

model VendorSettlement {
  id               String           @id @default(uuid())
  vendorId         String
  restaurantId     String?
  orderId          String?
  grossAmount      Decimal          @default(0) @db.Decimal(10, 2)
  commissionRate   Decimal?         @db.Decimal(5, 2)
  commissionAmount Decimal          @default(0) @db.Decimal(10, 2)
  netAmount        Decimal          @default(0) @db.Decimal(10, 2)
  status           SettlementStatus @default(PENDING)
  periodStart      DateTime?
  periodEnd        DateTime?
  processedAt      DateTime?
  paidAt           DateTime?
  transactionRef   String?
  failureReason    String?
  note             String?
  createdAt        DateTime         @default(now())
  updatedAt        DateTime         @updatedAt
  order            Order?           @relation(fields: [orderId], references: [id])
  restaurant       Restaurant?      @relation(fields: [restaurantId], references: [id])
  vendor           User             @relation("VendorSettlementUser", fields: [vendorId], references: [id], onDelete: Cascade)

  @@unique([orderId])
  @@index([vendorId])
  @@index([restaurantId])
  @@index([status])
  @@index([createdAt])
  @@index([periodStart, periodEnd])
  @@index([vendorId, status, createdAt])
}

model RiderSettlement {
  id             String           @id @default(uuid())
  riderId        String
  orderId        String?
  amount         Decimal          @default(0) @db.Decimal(10, 2)
  status         SettlementStatus @default(PENDING)
  periodStart    DateTime?
  periodEnd      DateTime?
  processedAt    DateTime?
  paidAt         DateTime?
  transactionRef String?
  failureReason  String?
  note           String?
  createdAt      DateTime         @default(now())
  updatedAt      DateTime         @updatedAt
  order          Order?           @relation(fields: [orderId], references: [id])
  rider          User             @relation("RiderSettlementUser", fields: [riderId], references: [id], onDelete: Cascade)

  @@unique([orderId])
  @@index([riderId])
  @@index([status])
  @@index([createdAt])
  @@index([periodStart, periodEnd])
  @@index([riderId, status, createdAt])
}

model RiderIncentive {
  id              String   @id @default(uuid())
  riderId         String
  title           String
  amount          Decimal  @db.Decimal(10, 2)
  targetOrders    Int
  completedOrders Int      @default(0)
  isCompleted     Boolean  @default(false)
  startDate       DateTime
  endDate         DateTime
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  rider           User     @relation(fields: [riderId], references: [id], onDelete: Cascade)

  @@index([riderId])
  @@index([isCompleted])
  @@index([startDate])
  @@index([endDate])
}

model UserPreference {
  id               String   @id @default(uuid())
  userId           String   @unique
  pushEnabled      Boolean  @default(true)
  orderUpdates     Boolean  @default(true)
  offersEnabled    Boolean  @default(true)
  smsEnabled       Boolean  @default(true)
  emailEnabled     Boolean  @default(true)
  marketingEnabled Boolean  @default(true)
  language         String   @default("en") @db.VarChar(20)
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
  user             User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
}

model AuthSession {
  id               String    @id @default(uuid())
  userId           String
  refreshTokenHash String    @unique
  deviceId         String?
  platform         String?   @db.VarChar(30)
  deviceName       String?
  ipAddress        String?
  userAgent        String?
  lastUsedAt       DateTime  @default(now())
  expiresAt        DateTime
  revokedAt        DateTime?
  createdAt        DateTime  @default(now())
  user             User      @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([deviceId])
  @@index([expiresAt])
  @@index([revokedAt])
  @@index([userId, revokedAt])
}

model SavedPaymentMethod {
  id            String        @id @default(uuid())
  userId        String
  method        PaymentMethod
  provider      String?       @db.VarChar(80)
  providerToken String?
  maskedNumber  String?       @db.VarChar(30)
  brand         String?       @db.VarChar(50)
  upiIdMasked   String?       @db.VarChar(120)
  expiryMonth   Int?
  expiryYear    Int?
  isDefault     Boolean       @default(false)
  isActive      Boolean       @default(true)
  createdAt     DateTime      @default(now())
  updatedAt     DateTime      @updatedAt
  user          User          @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([method])
  @@index([isDefault])
  @@index([isActive])
}

model SearchHistory {
  id        String   @id @default(uuid())
  userId    String
  query     String   @db.VarChar(200)
  createdAt DateTime @default(now())
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([createdAt])
  @@index([userId, createdAt])
}

model RestaurantMember {
  id           String               @id @default(uuid())
  restaurantId String
  userId       String
  role         RestaurantMemberRole @default(STAFF)
  permissions  Json?
  isActive     Boolean              @default(true)
  invitedAt    DateTime             @default(now())
  joinedAt     DateTime?
  createdAt    DateTime             @default(now())
  updatedAt    DateTime             @updatedAt
  restaurant   Restaurant           @relation(fields: [restaurantId], references: [id], onDelete: Cascade)
  user         User                 @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([restaurantId, userId])
  @@index([restaurantId])
  @@index([userId])
  @@index([role])
  @@index([isActive])
}

model VendorDocument {
  id              String                     @id @default(uuid())
  restaurantId    String
  type            VendorDocumentType
  documentNumber  String?
  documentUrl     String?
  frontImageUrl   String?
  backImageUrl    String?
  status          DocumentVerificationStatus @default(PENDING)
  rejectionReason String?
  reviewedById    String?
  reviewedAt      DateTime?
  expiresAt       DateTime?
  createdAt       DateTime                   @default(now())
  updatedAt       DateTime                   @updatedAt
  restaurant      Restaurant                 @relation(fields: [restaurantId], references: [id], onDelete: Cascade)
  reviewedBy      User?                      @relation("VendorDocumentReviewer", fields: [reviewedById], references: [id], onDelete: SetNull)

  @@index([restaurantId])
  @@index([type])
  @@index([status])
  @@index([expiresAt])
  @@index([reviewedById])
}

model VendorPayoutAccount {
  id                     String     @id @default(uuid())
  restaurantId           String     @unique
  accountHolder          String?
  bankName               String?
  accountNumberEncrypted String?
  accountLast4           String?    @db.VarChar(4)
  ifscCode               String?    @db.VarChar(20)
  upiId                  String?    @db.VarChar(150)
  isVerified             Boolean    @default(false)
  verifiedAt             DateTime?
  createdAt              DateTime   @default(now())
  updatedAt              DateTime   @updatedAt
  restaurant             Restaurant @relation(fields: [restaurantId], references: [id], onDelete: Cascade)

  @@index([isVerified])
}

model VendorOperatingException {
  id           String     @id @default(uuid())
  restaurantId String
  date         DateTime   @db.Date
  isClosed     Boolean    @default(true)
  openTime     String?
  closeTime    String?
  reason       String?
  createdAt    DateTime   @default(now())
  updatedAt    DateTime   @updatedAt
  restaurant   Restaurant @relation(fields: [restaurantId], references: [id], onDelete: Cascade)

  @@unique([restaurantId, date])
  @@index([restaurantId])
  @@index([date])
}

model RiderProfile {
  id                    String                  @id @default(uuid())
  userId                String                  @unique
  availabilityStatus    RiderAvailabilityStatus @default(OFFLINE)
  currentOrderCapacity  Int                     @default(1)
  maxOrderCapacity      Int                     @default(1)
  emergencyContactName  String?
  emergencyContactPhone String?                 @db.VarChar(20)
  bloodGroup            String?                 @db.VarChar(10)
  bankAccountHolder     String?
  bankAccountEncrypted  String?
  bankAccountLast4      String?                 @db.VarChar(4)
  bankName              String?
  ifscCode              String?                 @db.VarChar(20)
  upiId                 String?                 @db.VarChar(150)
  payoutVerified        Boolean                 @default(false)
  payoutVerifiedAt      DateTime?
  rating                Decimal                 @default(0) @db.Decimal(3, 2)
  totalRatings          Int                     @default(0)
  totalDeliveries       Int                     @default(0)
  cancellationCount     Int                     @default(0)
  joinedAt              DateTime                @default(now())
  createdAt             DateTime                @default(now())
  updatedAt             DateTime                @updatedAt
  user                  User                    @relation("RiderProfileUser", fields: [userId], references: [id], onDelete: Cascade)

  @@index([availabilityStatus])
  @@index([payoutVerified])
  @@index([rating])
}

model RiderDocument {
  id              String                     @id @default(uuid())
  riderId         String
  type            RiderDocumentType
  documentNumber  String?
  frontImageUrl   String?
  backImageUrl    String?
  documentUrl     String?
  status          DocumentVerificationStatus @default(PENDING)
  rejectionReason String?
  reviewedById    String?
  reviewedAt      DateTime?
  expiresAt       DateTime?
  createdAt       DateTime                   @default(now())
  updatedAt       DateTime                   @updatedAt
  rider           User                       @relation("RiderDocumentRider", fields: [riderId], references: [id], onDelete: Cascade)
  reviewedBy      User?                      @relation("RiderDocumentReviewer", fields: [reviewedById], references: [id], onDelete: SetNull)

  @@index([riderId])
  @@index([type])
  @@index([status])
  @@index([expiresAt])
  @@index([reviewedById])
}

model RiderAvailabilityLog {
  id             String    @id @default(uuid())
  riderId        String
  onlineAt       DateTime  @default(now())
  offlineAt      DateTime?
  startLatitude  Decimal?  @db.Decimal(10, 7)
  startLongitude Decimal?  @db.Decimal(10, 7)
  endLatitude    Decimal?  @db.Decimal(10, 7)
  endLongitude   Decimal?  @db.Decimal(10, 7)
  createdAt      DateTime  @default(now())
  rider          User      @relation("RiderAvailabilityUser", fields: [riderId], references: [id], onDelete: Cascade)

  @@index([riderId])
  @@index([onlineAt])
  @@index([offlineAt])
  @@index([riderId, onlineAt])
}

model RiderRating {
  id        String   @id @default(uuid())
  riderId   String
  userId    String
  orderId   String   @unique
  rating    Int
  review    String?
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  rider     User     @relation("RatedRider", fields: [riderId], references: [id], onDelete: Cascade)
  user      User     @relation("RiderRatingCustomer", fields: [userId], references: [id], onDelete: Cascade)
  order     Order    @relation(fields: [orderId], references: [id], onDelete: Cascade)

  @@index([riderId])
  @@index([userId])
  @@index([rating])
  @@index([isActive])
}

model VendorInvoice {
  id               String              @id @default(uuid())
  invoiceNumber    String              @unique @db.VarChar(80)
  vendorId         String
  restaurantId     String
  periodStart      DateTime
  periodEnd        DateTime
  totalOrders      Int                 @default(0)
  grossAmount      Decimal             @default(0) @db.Decimal(12, 2)
  commissionAmount Decimal             @default(0) @db.Decimal(12, 2)
  taxAmount        Decimal             @default(0) @db.Decimal(12, 2)
  adjustmentAmount Decimal             @default(0) @db.Decimal(12, 2)
  netPayable       Decimal             @default(0) @db.Decimal(12, 2)
  status           InvoiceStatus       @default(DRAFT)
  generatedAt      DateTime            @default(now())
  dueAt            DateTime?
  paidAt           DateTime?
  pdfUrl           String?
  note             String?
  createdAt        DateTime            @default(now())
  updatedAt        DateTime            @updatedAt
  vendor           User                @relation("VendorInvoiceUser", fields: [vendorId], references: [id], onDelete: Restrict)
  restaurant       Restaurant          @relation(fields: [restaurantId], references: [id], onDelete: Restrict)
  items            VendorInvoiceItem[]

  @@unique([restaurantId, periodStart, periodEnd])
  @@index([vendorId])
  @@index([restaurantId])
  @@index([status])
  @@index([periodStart, periodEnd])
  @@index([generatedAt])
}

model VendorInvoiceItem {
  id                   String        @id @default(uuid())
  invoiceId            String
  orderId              String
  orderNumber          String        @db.VarChar(50)
  deliveredAt          DateTime?
  paymentMethod        PaymentMethod
  paymentStatus        PaymentStatus
  grossAmount          Decimal       @db.Decimal(10, 2)
  commissionableAmount Decimal       @db.Decimal(10, 2)
  commissionRate       Decimal       @db.Decimal(5, 2)
  commissionAmount     Decimal       @db.Decimal(10, 2)
  vendorPayable        Decimal       @db.Decimal(10, 2)
  deliveryFee          Decimal       @default(0) @db.Decimal(10, 2)
  taxAmount            Decimal       @default(0) @db.Decimal(10, 2)
  discountAmount       Decimal       @default(0) @db.Decimal(10, 2)
  platformFee          Decimal       @default(0) @db.Decimal(10, 2)
  refundAmount         Decimal       @default(0) @db.Decimal(10, 2)
  createdAt            DateTime      @default(now())
  invoice              VendorInvoice @relation(fields: [invoiceId], references: [id], onDelete: Cascade)
  order                Order         @relation(fields: [orderId], references: [id], onDelete: Restrict)

  @@unique([invoiceId, orderId])
  @@index([orderId])
  @@index([paymentMethod])
  @@index([paymentStatus])
  @@index([deliveredAt])
}

model AdminProfile {
  id           String                 @id @default(uuid())
  userId       String                 @unique
  designation  String?
  isSuperAdmin Boolean                @default(false)
  notes        String?
  lastLoginAt  DateTime?
  createdAt    DateTime               @default(now())
  updatedAt    DateTime               @updatedAt
  user         User                   @relation("AdminProfileUser", fields: [userId], references: [id], onDelete: Cascade)
  permissions  AdminPermissionGrant[]

  @@index([userId])
  @@index([isSuperAdmin])
}

model AdminPermissionGrant {
  id             String          @id @default(uuid())
  adminProfileId String
  permission     AdminPermission
  createdAt      DateTime        @default(now())
  adminProfile   AdminProfile    @relation(fields: [adminProfileId], references: [id], onDelete: Cascade)

  @@unique([adminProfileId, permission])
  @@index([permission])
}

model AdminAuditLog {
  id         String   @id @default(uuid())
  adminId    String?
  action     String   @db.VarChar(120)
  entityType String   @db.VarChar(120)
  entityId   String?
  oldData    Json?
  newData    Json?
  metadata   Json?
  ipAddress  String?
  userAgent  String?
  createdAt  DateTime @default(now())
  admin      User?    @relation("AdminAuditActor", fields: [adminId], references: [id], onDelete: SetNull)

  @@index([adminId])
  @@index([action])
  @@index([entityType])
  @@index([entityId])
  @@index([createdAt])
}

model PaymentTransaction {
  id                String                   @id @default(uuid())
  orderId           String
  amount            Decimal                  @db.Decimal(10, 2)
  gatewayFee        Decimal                  @default(0) @db.Decimal(10, 2)
  gatewayTax        Decimal                  @default(0) @db.Decimal(10, 2)
  netReceived       Decimal?                 @db.Decimal(10, 2)
  currency          String                   @default("INR") @db.VarChar(10)
  method            PaymentMethod
  status            PaymentTransactionStatus @default(CREATED)
  provider          String?                  @db.VarChar(80)
  providerOrderId   String?
  providerPaymentId String?
  providerSignature String?
  failureCode       String?
  failureReason     String?
  rawResponse       Json?
  paidAt            DateTime?
  failedAt          DateTime?
  createdAt         DateTime                 @default(now())
  updatedAt         DateTime                 @updatedAt
  order             Order                    @relation(fields: [orderId], references: [id], onDelete: Cascade)
  refunds           Refund[]

  @@index([orderId])
  @@index([status])
  @@index([method])
  @@index([provider])
  @@index([providerOrderId])
  @@index([providerPaymentId])
  @@index([createdAt])
}

model Refund {
  id                   String              @id @default(uuid())
  orderId              String
  paymentTransactionId String?
  processedByAdminId   String?
  amount               Decimal             @db.Decimal(10, 2)
  reason               String?
  status               RefundStatus        @default(PENDING)
  providerRefundId     String?             @unique
  failureReason        String?
  requestedAt          DateTime            @default(now())
  processedAt          DateTime?
  completedAt          DateTime?
  createdAt            DateTime            @default(now())
  updatedAt            DateTime            @updatedAt
  order                Order               @relation(fields: [orderId], references: [id], onDelete: Cascade)
  paymentTransaction   PaymentTransaction? @relation(fields: [paymentTransactionId], references: [id], onDelete: SetNull)
  processedByAdmin     User?               @relation("RefundProcessedByAdmin", fields: [processedByAdminId], references: [id], onDelete: SetNull)

  @@index([orderId])
  @@index([paymentTransactionId])
  @@index([processedByAdminId])
  @@index([status])
  @@index([createdAt])
}

model SystemSetting {
  id          String   @id @default(uuid())
  key         String   @unique @db.VarChar(120)
  value       Json
  description String?
  isPublic    Boolean  @default(false)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([key])
  @@index([isPublic])
}

model Banner {
  id        String    @id @default(uuid())
  title     String
  subtitle  String?
  imageUrl  String    @db.VarChar(550)
  deepLink  String?
  placement String    @default("HOME") @db.VarChar(80)
  sortOrder Int       @default(0)
  isActive  Boolean   @default(true)
  startsAt  DateTime?
  endsAt    DateTime?
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt

  @@index([placement])
  @@index([isActive])
  @@index([startsAt])
  @@index([endsAt])
  @@index([sortOrder])
}

enum RestaurantMemberRole {
  OWNER
  MANAGER
  STAFF
  ACCOUNTANT
}

enum DocumentVerificationStatus {
  PENDING
  UNDER_REVIEW
  APPROVED
  REJECTED
  EXPIRED
}

enum VendorDocumentType {
  FSSAI
  GST
  PAN
  BANK_PROOF
  SHOP_LICENSE
  OWNER_ID
  OTHER
}

enum RiderDocumentType {
  AADHAAR
  PAN
  DRIVING_LICENSE
  RC
  INSURANCE
  POLLUTION_CERTIFICATE
  BANK_PROOF
  PROFILE_PHOTO
  OTHER
}

enum RiderAvailabilityStatus {
  OFFLINE
  AVAILABLE
  BUSY
  PAUSED
  SUSPENDED
}

enum InvoiceStatus {
  DRAFT
  GENERATED
  APPROVED
  PROCESSING
  PAID
  CANCELLED
}

enum VendorVerificationStatus {
  PENDING
  UNDER_REVIEW
  APPROVED
  REJECTED
  SUSPENDED
}

enum PaymentTransactionStatus {
  CREATED
  PENDING
  SUCCESS
  FAILED
  REFUNDED
  PARTIALLY_REFUNDED
  CANCELLED
}

enum RefundStatus {
  PENDING
  PROCESSING
  COMPLETED
  FAILED
  CANCELLED
}

enum AdminPermission {
  DASHBOARD_VIEW
  USER_VIEW
  USER_MANAGE
  VENDOR_VIEW
  VENDOR_MANAGE
  RIDER_VIEW
  RIDER_MANAGE
  ORDER_VIEW
  ORDER_MANAGE
  BILLING_VIEW
  SETTLEMENT_MANAGE
  REFUND_MANAGE
  COUPON_MANAGE
  CATEGORY_MANAGE
  NOTIFICATION_MANAGE
  SUPPORT_MANAGE
  SERVICE_AREA_MANAGE
  REPORT_VIEW
  AUDIT_VIEW
  SETTINGS_MANAGE
  ADMIN_MANAGE
}

enum Role {
  CUSTOMER
  VENDOR
  RIDER
  ADMIN
}

enum OrderStatus {
  PLACED
  ACCEPTED_BY_VENDOR
  PREPARING
  READY_FOR_PICKUP
  ASSIGNED_TO_RIDER
  PICKED_UP
  OUT_FOR_DELIVERY
  DELIVERED
  CANCELLED
}

enum PaymentStatus {
  PENDING
  PAID
  FAILED
  REFUNDED
  PARTIALLY_REFUNDED
}

enum PaymentMethod {
  COD
  ONLINE
  UPI
  CARD
  WALLET
}

enum CouponType {
  PERCENT
  FLAT
  FREE_DELIVERY
}

enum CouponScope {
  GLOBAL
  RESTAURANT
  CITY
  FIRST_ORDER
}

enum WalletTransactionType {
  CREDIT
  DEBIT
  REFUND
  ORDER_PAYMENT
  RIDER_EARNING
  REFERRAL_REWARD
  ADJUSTMENT
}

enum SettlementStatus {
  PENDING
  PROCESSING
  PAID
  FAILED
  CANCELLED
}

enum SupportTicketStatus {
  OPEN
  IN_PROGRESS
  RESOLVED
  CLOSED
}

enum SupportTicketPriority {
  LOW
  MEDIUM
  HIGH
  URGENT
}

enum NotificationType {
  ORDER
  PAYMENT
  OFFER
  SUPPORT
  REFERRAL
  WALLET
  SYSTEM
}

enum ReferralStatus {
  PENDING
  COMPLETED
  REWARDED
  CANCELLED
}

model ServiceArea {
  id String @id @default(uuid())

  city    String  @db.VarChar(100)
  state   String? @db.VarChar(100)
  country String  @default("India") @db.VarChar(100)
  pinCode String? @db.VarChar(10)

  latitude  Decimal @db.Decimal(10, 7)
  longitude Decimal @db.Decimal(10, 7)
  radiusKm  Decimal @db.Decimal(8, 2)

  isActive Boolean @default(true)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([isActive])
  @@index([city])
  @@index([pinCode])
}

// ======================================================
// KARTOMART
// Additive models only.
// Existing Restaurant/MenuItem/CartItem/Order untouched.
// ======================================================

enum MartStoreType {
  GROCERY
  FRUITS_VEGETABLES
  SUPERMARKET
  GENERAL_STORE
}

enum MartProductUnit {
  G
  KG
  ML
  L
  PCS
  PACK
  BOX
  DOZEN
}

enum MartOrderStatus {
  PLACED
  ACCEPTED_BY_VENDOR
  PACKING
  READY_FOR_PICKUP
  ASSIGNED_TO_RIDER
  PICKED_UP
  OUT_FOR_DELIVERY
  DELIVERED
  CANCELLED
}

// ======================================================
// MART STORE
// ======================================================

model MartStore {
  id       String  @id @default(uuid())
  vendorId String?
  cityId   String?

  name String        @db.VarChar(255)
  type MartStoreType @default(GROCERY)

  description String?
  address     String  @db.VarChar(255)
  phone       String? @db.VarChar(20)
  email       String? @db.VarChar(255)

  imageUrl  String? @db.VarChar(550)
  bannerUrl String? @db.VarChar(550)

  latitude  Decimal? @db.Decimal(10, 7)
  longitude Decimal? @db.Decimal(10, 7)

  rating       Decimal @default(0) @db.Decimal(3, 2)
  totalReviews Int     @default(0)

  deliveryFee  Decimal @default(0) @db.Decimal(10, 2)
  minimumOrder Decimal @default(0) @db.Decimal(10, 2)
  deliveryTime String  @default("30-45 mins") @db.VarChar(50)

  isOpen            Boolean @default(true)
  isFeatured        Boolean @default(false)
  isAcceptingOrders Boolean @default(true)
  isVerified        Boolean @default(false)

  openingTime  String?
  closingTime  String?
  weeklyOffDay String?

  commission Decimal @default(0) @db.Decimal(5, 2)

  deletedAt DateTime?
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt

  categories MartCategory[]
  products   MartProduct[]
  cartItems  MartCartItem[]
  orders     MartOrder[]

  @@index([vendorId])
  @@index([cityId])
  @@index([type])
  @@index([isOpen])
  @@index([isFeatured])
  @@index([isAcceptingOrders])
  @@index([isVerified])
  @@index([createdAt])
  @@index([deletedAt])
}

// ======================================================
// MART CATEGORY
// ======================================================

model MartCategory {
  id      String @id @default(uuid())
  storeId String

  name        String  @db.VarChar(120)
  description String?
  imageUrl    String? @db.VarChar(550)

  sortOrder Int     @default(0)
  isActive  Boolean @default(true)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  store MartStore @relation(fields: [storeId], references: [id], onDelete: Cascade)

  products MartProduct[]

  @@index([storeId])
  @@index([name])
  @@index([isActive])
  @@index([sortOrder])
}

// ======================================================
// MART PRODUCT
// Example:
// Tomato
// Aashirvaad Atta
// Amul Milk
// ======================================================

model MartProduct {
  id         String  @id @default(uuid())
  storeId    String
  categoryId String?

  name        String  @db.VarChar(255)
  description String?
  brand       String? @db.VarChar(150)

  imageUrl String? @db.VarChar(550)

  isAvailable Boolean @default(true)
  isFeatured  Boolean @default(false)
  isPopular   Boolean @default(false)

  rating       Decimal @default(0) @db.Decimal(3, 2)
  totalReviews Int     @default(0)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  store    MartStore     @relation(fields: [storeId], references: [id], onDelete: Cascade)
  category MartCategory? @relation(fields: [categoryId], references: [id], onDelete: SetNull)

  variants   MartProductVariant[]
  cartItems  MartCartItem[]
  orderItems MartOrderItem[]

  @@index([storeId])
  @@index([categoryId])
  @@index([name])
  @@index([brand])
  @@index([isAvailable])
  @@index([isFeatured])
  @@index([isPopular])
}

// ======================================================
// MART PRODUCT VARIANT
//
// Tomato:
// 250 G
// 500 G
// 1 KG
//
// Milk:
// 500 ML
// 1 L
// ======================================================

model MartProductVariant {
  id        String @id @default(uuid())
  productId String

  label String @db.VarChar(100)

  quantity Decimal         @db.Decimal(10, 3)
  unit     MartProductUnit

  mrp   Decimal @db.Decimal(10, 2)
  price Decimal @db.Decimal(10, 2)

  stock Int @default(0)

  sku     String? @unique @db.VarChar(120)
  barcode String? @db.VarChar(150)

  isDefault   Boolean @default(false)
  isAvailable Boolean @default(true)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  product MartProduct @relation(fields: [productId], references: [id], onDelete: Cascade)

  cartItems  MartCartItem[]
  orderItems MartOrderItem[]

  @@index([productId])
  @@index([unit])
  @@index([isAvailable])
  @@index([barcode])
}

// ======================================================
// MART CART
// Existing food CartItem remains unchanged.
// ======================================================

model MartCartItem {
  id String @id @default(uuid())

  userId    String
  storeId   String
  productId String
  variantId String

  quantity Int @default(1)

  price      Decimal @db.Decimal(10, 2)
  totalPrice Decimal @db.Decimal(10, 2)

  note String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  store   MartStore          @relation(fields: [storeId], references: [id], onDelete: Cascade)
  product MartProduct        @relation(fields: [productId], references: [id], onDelete: Cascade)
  variant MartProductVariant @relation(fields: [variantId], references: [id], onDelete: Cascade)

  @@unique([userId, variantId])
  @@index([userId])
  @@index([storeId])
  @@index([productId])
  @@index([variantId])
}

// ======================================================
// MART ORDER
// Separate from existing food Order.
// ======================================================

model MartOrder {
  id String @id @default(uuid())

  userId    String
  storeId   String
  vendorId  String?
  riderId   String?
  addressId String?

  orderNumber String @unique @db.VarChar(50)

  itemTotal   Decimal @default(0) @db.Decimal(10, 2)
  deliveryFee Decimal @default(0) @db.Decimal(10, 2)
  discount    Decimal @default(0) @db.Decimal(10, 2)
  taxAmount   Decimal @default(0) @db.Decimal(10, 2)
  platformFee Decimal @default(0) @db.Decimal(10, 2)

  totalAmount Decimal @db.Decimal(10, 2)

  paymentMethod PaymentMethod @default(COD)
  paymentStatus PaymentStatus @default(PENDING)

  status MartOrderStatus @default(PLACED)

  customerNote String?

  acceptedAt  DateTime?
  packingAt   DateTime?
  readyAt     DateTime?
  pickedAt    DateTime?
  deliveredAt DateTime?
  cancelledAt DateTime?

  cancelReason String?
  cancelledBy  String?

  distanceKm Decimal? @db.Decimal(10, 2)

  commissionableAmount     Decimal? @db.Decimal(10, 2)
  commissionRate           Decimal? @db.Decimal(5, 2)
  platformCommissionAmount Decimal? @db.Decimal(10, 2)
  vendorSettlementAmount   Decimal? @db.Decimal(10, 2)

  deliveryOtp          String?
  deliveryOtpExpiresAt DateTime?
  deliveryOtpVerified  Boolean   @default(false)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  store MartStore @relation(fields: [storeId], references: [id])

  items   MartOrderItem[]
  history MartOrderStatusHistory[]

  @@index([userId])
  @@index([storeId])
  @@index([vendorId])
  @@index([riderId])
  @@index([status])
  @@index([paymentStatus])
  @@index([paymentMethod])
  @@index([createdAt])
  @@index([deliveredAt])
  @@index([cancelledAt])
  @@index([storeId, status, createdAt])
  @@index([riderId, status, deliveredAt])
}

// ======================================================
// MART ORDER ITEM
// Snapshot keeps historical order information unchanged
// even if product/variant is later changed.
// ======================================================

model MartOrderItem {
  id String @id @default(uuid())

  orderId   String
  productId String?
  variantId String?

  itemName     String  @db.VarChar(255)
  variantLabel String? @db.VarChar(100)

  quantity Int

  price      Decimal  @db.Decimal(10, 2)
  mrp        Decimal? @db.Decimal(10, 2)
  totalPrice Decimal  @db.Decimal(10, 2)

  imageUrl String? @db.VarChar(550)

  createdAt DateTime @default(now())

  order   MartOrder           @relation(fields: [orderId], references: [id], onDelete: Cascade)
  product MartProduct?        @relation(fields: [productId], references: [id], onDelete: SetNull)
  variant MartProductVariant? @relation(fields: [variantId], references: [id], onDelete: SetNull)

  @@index([orderId])
  @@index([productId])
  @@index([variantId])
}

// ======================================================
// MART ORDER STATUS HISTORY
// ======================================================

model MartOrderStatusHistory {
  id      String          @id @default(uuid())
  orderId String
  status  MartOrderStatus

  changedBy String?
  note      String?

  createdAt DateTime @default(now())

  order MartOrder @relation(fields: [orderId], references: [id], onDelete: Cascade)

  @@index([orderId])
  @@index([status])
  @@index([changedBy])
}
